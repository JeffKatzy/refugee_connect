- if @matches.present?
	%h2= "Matches for #{@auth.name}"
	Your matching appointments:
	= form_tag "/appointments/batch", method: "post" do
		= hidden_field_tag 'user_id', params[:user_id]
		- @matches.each_with_index do |match, idx|
			%br
			%span= "#{match[:user_name]}'s availabilities"
			- match[:times].each do |time|
				%p
				%span= time.in_time_zone('Eastern Time (US & Canada)').strftime("%I:%M%p on %As") + " EST"
				= check_box_tag "[match_id][#{match[:user_id]}]", "#{time}", id: "#{match[:user_name]}.#{time.in_time_zone('Eastern Time (US & Canada)').strftime("%I:%M%p on %As")}"
		%br
		%p= submit_tag "Sign up for classes"
- else
	There are no available appointments for you this week.